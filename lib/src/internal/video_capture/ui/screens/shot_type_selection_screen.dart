import 'package:flutter/material.dart';

class ShotTypeSelectionScreen extends StatelessWidget {
  const ShotTypeSelectionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
